lora_config:
  r: 16
  lora_alpha: 32
  lora_dropout: 0.5
  bias: "none"
  task_type: "CAUSAL_LM"

dataset:
  name: 
    TA: "Xcvddax/Attack-techniques"
    ATT: "Xcvddax/Attack-techniques"
  split: "train"

model:
  name: "google/gemma-7b"
  finetune_model: "attack-gemma-7b"
  
api_tokens:
  huggingface: "HUGGINGFACE_API_TOKEN"

training_args:
  output_dir: "attack-gemma-7b"
  per_device_train_batch_size: 1
  gradient_accumulation_steps: 4
  logging_steps: 20
  push_to_hub: True

trainer:
  dataset_text_field: "text"
  max_seq_length: 512

fine_tuning:
  method: "full" # "lora" or "full"
